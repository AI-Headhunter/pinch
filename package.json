{
  "name": "pinch-workspace",
  "private": true,
  "scripts": {
    "build": "pnpm --dir gen/ts build && pnpm --dir skill build",
    "test:install-smoke": "bash tests/install-smoke.sh"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3",
      "esbuild",
      "@biomejs/biome"
    ]
  }
}
